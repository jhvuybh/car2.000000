# 中心平衡零点设置

平衡系统有三个不同的“零点”，不要混在一起：

1. **滑轨机械零点**：下限位开关位置，用回零模式3建立。
2. **水管水平位置**：从机械零点移动到水管水平时的电机位置，记为`LEVEL_STEPS`。
3. **钢球目标中心**：K230坐标0~1000中的目标值，通常是`BALL_TARGET=500`。

## 推荐校准流程

1. 滑轨移动到行程中间，低速调试。
2. 执行`zdt_stepper_start_homing(3)`回到下限位。
3. 确认回零完成后，用每次20~100脉冲的小步进向上移动。
4. 用水平仪粗调水管水平；再放入钢球，观察钢球在无控制时是否持续向一侧滚。
5. 继续用小步进微调，直到钢球平均漂移速度最小。
6. 读取当前位置，并换算成命令脉冲，记录为`LEVEL_STEPS_FROM_HOME`。
7. 每次开机都先回零，再移动到这个绝对位置，然后启动钢球PD控制。

```c
#define LEVEL_STEPS_FROM_HOME  12000L  /* 仅为占位，必须换成实测值 */
#define BALL_TARGET_PIXEL_NORM 500.0f  /* 画面0~1000的中心，不是5cm高度 */

zdt_stepper_start_homing(3);
/* 等待并确认回零成功，不能只依靠固定延时。 */
zdt_stepper_move_absolute(LEVEL_STEPS_FROM_HOME, 60, 20);
```

## 手动微调

```c
/* 每次向正方向移动50脉冲。 */
zdt_stepper_move_relative(50, 30, 20);

/* 每次向负方向移动50脉冲。 */
zdt_stepper_move_relative(-50, 30, 20);
```

如果正数实际让滑轨向下，在`zdt_stepper.h`中把
`ZDT_STEPPER_POSITIVE_DIR`从0改成1。

## 从实时位置得到LEVEL_STEPS

电机返回的位置单位是65536单位/圈，位置命令默认16细分时是3200脉冲/圈：

```c
zdt_stepper_feedback_t feedback;
int32_t level_steps;

zdt_stepper_request_position();
/* 主循环持续调用zdt_stepper_poll()，收到回复后再读取。 */
zdt_stepper_get_feedback(&feedback);

if (feedback.position_valid) {
    level_steps = (int32_t)(
        (int64_t)feedback.position_units * 3200LL / 65536LL
    );
}
```

将调平后得到的`level_steps`记录下来，写入
`LEVEL_STEPS_FROM_HOME`。这种方法保留了下限位机械坐标，断电后可以重复恢复，
比每次人工调平更可靠。

## 可选：直接把水平位置清零

水管调平后也可以执行：

```c
zdt_stepper_set_current_position_zero();
```

此后水平位置就是0，平衡目标可以在0附近正负变化。但断电后仍必须先回机械
零点并重新移动到水平位置，所以正式比赛更建议保存`LEVEL_STEPS_FROM_HOME`。

## 钢球中心

K230程序发送的是沿水管方向的归一化位置0~1000，因此画面中心默认是500：

```c
float ball_error = BALL_TARGET_PIXEL_NORM - ball_position;
```

如果水管有效区域只占画面的一部分，测出水管左、右端坐标后使用：

```text
BALL_TARGET = (左端坐标 + 右端坐标) / 2
```

水管水平零点与钢球画面中心不是同一个参数：前者决定滑轨基础高度，后者决定
钢球停在哪里。单轴Z轴陀螺仪只能测水管角速度，不能可靠给出静态水平角度，
因此不能代替机械回零与水平位置标定。
